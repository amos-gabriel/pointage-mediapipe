# Système de Pointage Facial — Dépendances Python
# Installer avec : pip install -r requirements.txt
# Prérequis : cmake installé sur le système (pour dlib/face_recognition)

# Web Framework
flask>=3.0.0,<4.0.0
flask-sqlalchemy>=3.1.0,<4.0.0
flask-migrate>=4.0.0,<5.0.0

# Vision & IA
opencv-python>=4.9.0,<5.0.0
face_recognition>=1.3.0         # Nécessite cmake + dlib
mediapipe==0.10.14              # Version stable avec solutions API (Python 3.10)
numpy>=1.26.0,<2.0.0

# Image
Pillow>=10.0.0,<11.0.0

# Utilitaires
python-dotenv>=1.0.0,<2.0.0
setuptools==69.5.1              # Requis pour pkg_resources (face_recognition_models)

# Tests
pytest>=8.0.0,<9.0.0
pytest-cov>=5.0.0,<6.0.0

# Dev uniquement (retirer en production)
# flask-debugtoolbar==0.14.1
